name: PB-3321 release bare conformance

on: release

permissions:
  contents: read

jobs:
  observe:
    runs-on: ubuntu-latest
    steps:
      - run: printf '%s:%s\n' "$GITHUB_EVENT_NAME" "${{ github.event.action }}"

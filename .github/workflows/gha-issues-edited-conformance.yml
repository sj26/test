name: GHA issues edited conformance

on:
  issues:
    types: [edited]

permissions:
  contents: read

jobs:
  observe:
    runs-on: ubuntu-latest
    steps:
      - name: Record selected activity
        run: printf '%s\n' "$GITHUB_EVENT_NAME:$GITHUB_REF:$GITHUB_SHA"

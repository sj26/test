name: PR activity E2E A09 synchronize
on:
  pull_request:
    types: [synchronize]

permissions: {}

jobs:
  evidence:
    name: event evidence
    runs-on: ubuntu-latest
    steps:
      - name: Verify event context
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_EVENT_NAME" = pull_request
          test '${{ github.event.action }}' = 'synchronize'
          printf 'E2E_CASE=%s\n' 'A09'
          printf 'PULL_REQUEST_ACTION=%s\n' '${{ github.event.action }}'
          printf 'GITHUB_SHA=%s\n' "$GITHUB_SHA"

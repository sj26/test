name: PR activity E2E A17 ready_for_review
on:
  pull_request:
    types: [ready_for_review]

permissions: {}

jobs:
  evidence:
    name: event evidence
    runs-on: ubuntu-latest
    steps:
      - name: Verify event context
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_EVENT_NAME" = pull_request
          test '${{ github.event.action }}' = 'ready_for_review'
          printf 'E2E_CASE=%s\n' 'A17'
          printf 'PULL_REQUEST_ACTION=%s\n' '${{ github.event.action }}'
          printf 'GITHUB_SHA=%s\n' "$GITHUB_SHA"

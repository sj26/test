name: Skip instruction parity

on:
  push:
    branches: [gha-skip-parity-20260904-024732]
    tags: [gha-skip-parity-*]

jobs:
  evidence:
    runs-on: ubuntu-latest
    steps:
      - run: echo "RUN $GITHUB_SHA"

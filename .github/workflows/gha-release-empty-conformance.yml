name: PB-3321 release empty conformance

on:
  release:
    types: []

permissions:
  contents: read

jobs:
  observe:
    runs-on: ubuntu-latest
    steps:
      - run: printf '%s:%s\n' "$GITHUB_EVENT_NAME" "${{ github.event.action }}"
